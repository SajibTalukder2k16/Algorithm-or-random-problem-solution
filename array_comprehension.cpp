#include<bits/stdc++.h>
using namespace std;
int main()
{
   /** vector<int>vec[1000];
    int i,ara[12];
    for(i=0;i<12;i++)
    {
        cin>>ara[i];
    }
    for(i=0;i<12;i++)
    {
        vec[ara[i]].push_back(i);
    }
    vector<int>::iterator it;
    for(it=vec[2].begin();it!=vec[2].end();it++)
    {
        cout<<*it<<"\t";
    }**/
    //cout<<abs(1)<<endl;
    vector<int>vec(3);
    cout<<vec[0]<<endl;
}
